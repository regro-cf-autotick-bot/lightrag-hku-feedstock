{% set name = "lightrag-hku" %}
{% set version = "1.0.9" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  url: https://pypi.org/packages/source/{{ name[0] }}/{{ name }}/lightrag_hku-{{ version }}.tar.gz
  sha256: b3d05e64909943b9f5d1b55d1eee77391f61f5702e55c2ec5ce415adb004f16f
  patches:
    - requirements.patch

build:
  noarch: python
  script: {{ PYTHON }} -m pip install . -vv --no-deps --no-build-isolation
  number: 0

requirements:
  host:
    - python >=3.9
    - pip
    - setuptools
  run:
    - aioboto3
    - aiobotocore =2.16.0
    - ollama
    - pyvis
    - python >=3.9
    - accelerate
    - ollama-python
    - openai
    - tiktoken
    - networkx
    - graspologic
    - nano-vectordb
    - hnswlib
    - python-xxhash
    - pytorch
    - tenacity
    - transformers
    - neo4j-python-driver
    - oracledb

test:
  imports:
    - lightrag
  commands:
    - pip check
  requires:
    - pip

about:
  home: https://github.com/HKUDS/LightRAG
  summary: 'LightRAG: Simple and Fast Retrieval-Augmented Generation'
  license: MIT
  license_file: LICENSE

extra:
  recipe-maintainers:
    - jan-janssen
