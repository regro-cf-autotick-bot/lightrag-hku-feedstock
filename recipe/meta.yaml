{% set name = "lightrag-hku" %}
{% set version = "0.0.8" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  url: https://pypi.org/packages/source/{{ name[0] }}/{{ name }}/lightrag_hku-{{ version }}.tar.gz
  sha256: d4d6d93317d012f7e5db70d88c33d7fb9eb0e236e41cea56596f8c76164b6b08
  patches:
    - requirements.patch

build:
  noarch: python
  script: {{ PYTHON }} -m pip install . -vv --no-deps --no-build-isolation
  number: 0

requirements:
  host:
    - python >=3.9
    - pip
    - setuptools
  run:
    - aioboto3
    - ollama
    - pyvis
    - python >=3.9
    - accelerate
    - ollama-python
    - openai
    - tiktoken
    - networkx
    - graspologic
    - nano-vectordb
    - hnswlib
    - python-xxhash
    - pytorch
    - tenacity
    - transformers

test:
  imports:
    - lightrag
  commands:
    - pip check
  requires:
    - pip

about:
  home: https://github.com/HKUDS/LightRAG
  summary: 'LightRAG: Simple and Fast Retrieval-Augmented Generation'
  license: MIT
  license_file: LICENSE

extra:
  recipe-maintainers:
    - jan-janssen
